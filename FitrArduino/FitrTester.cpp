/*
#include <iostream>
#include <string>

#include "List.h"

using namespace std;

int main(int argc, char *argv[]) {
    List<int> numbers(1, 3);
    numbers.add(5, 6, 7);

    cout << "Showing" << endl;
    for(int i = 0; i < numbers.size(); i++) {
        cout << "\tNumber at " << i << " is " << numbers[i] << endl;
    }

    cout << "Removing at index 0" << endl;
    numbers.remove(0);
    for(int i = 0; i < numbers.size(); i++) {
        cout << "\tNumber at " << i << " is " << numbers[i] << endl;
    }

    cout << "Removing at index 1" << endl;
    numbers.remove(1);
    for(int i = 0; i < numbers.size(); i++) {
        cout << "\tNumber at " << i << " is " << numbers[i] << endl;
    }

    cout << "Adding at index 0" << endl;
    numbers.addAt(0, 99);
    for(int i = 0; i < numbers.size(); i++) {
        cout << "\tNumber at " << i << " is " << numbers[i] << endl;
    }

    cout << "Adding at index 2" << endl;
    numbers.addAt(2, 101);
    for(int i = 0; i < numbers.size(); i++) {
        cout << "\tNumber at " << i << " is " << numbers[i] << endl;
    }


    return 0;
}
*/
